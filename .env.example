# ====== LLM CONFIGURATION ======
# LLM_PROVIDER: e.g. azure, openai, anthropic, xai, ollama, huggingface
LLM_PROVIDER = 
LLM_MODEL = 
# Optional - leave blank or remove if the provider doesn't need them
LLM_API_KEY = 
LLM_API_BASE = 
LLM_API_VERSION = 

# ====== EMBEDDING CONFIGURATION ======
# EMBED_PROVIDER: e.g. openai, azure, cohere, gemini
EMBED_PROVIDER = 
EMBED_MODEL = 
# Optional - leave blank or remove if the provider doesn't need them
EMBED_API_KEY = 
EMBED_API_BASE = 
EMBED_API_VERSION = 

PERSIST_DIRECTORY = "./narrative_storage/chroma_db"
DATABASE_NAME = "./narrative_storage/narrative.db"
